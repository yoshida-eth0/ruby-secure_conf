require 'spec_helper'

describe SecureConf do
  it 'has a version number' do
    expect(SecureConf::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
